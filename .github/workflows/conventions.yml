name: Enforce conventions

on:
  pull_request:
    types: [opened]

jobs:
  run:
    uses: Friedinger/configs/.github/workflows/action-conventions.yml@6bf9f3725ccc8b59f39c62672cfd7b626ebba33c # v1.2.2
    permissions:
      pull-requests: write
